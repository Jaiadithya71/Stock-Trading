const AppConfig = {
    // Use relative URL for deployment (works on any domain)
    API_BASE_URL: '/api',
    REFRESH_INTERVAL: 60000, // 60 seconds
    AUTO_REFRESH: true,
    DEBUG: false // Disable debug in production
};